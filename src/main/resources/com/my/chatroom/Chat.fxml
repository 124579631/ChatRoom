<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Text?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.my.chatroom.ChatController"
            prefHeight="650.0" prefWidth="900.0"
            styleClass="root">

    <left>
        <VBox styleClass="user-list-pane" prefWidth="250" spacing="0">
            <HBox alignment="CENTER_LEFT" prefHeight="60" styleClass="sidebar-header">
                <padding><Insets left="20"/></padding>
                <Text text="消息" styleClass="header-text"/>
            </HBox>

            <ListView fx:id="userListView" VBox.vgrow="ALWAYS" styleClass="user-list-view"/>

            <Button text="📢  进入公共广场" maxWidth="Infinity"
                    styleClass="public-chat-btn"
                    onAction="#handleJoinPublicChat"/>
        </VBox>
    </left>

    <center>
        <VBox styleClass="chat-area-pane">
            <HBox fx:id="normalChatHeader" alignment="CENTER_LEFT" prefHeight="60" styleClass="chat-header">
                <padding><Insets left="20"/></padding>
                <Text fx:id="chatTargetLabel" text="未选择会话" styleClass="chat-title-text"/>
                <Region HBox.hgrow="ALWAYS"/>
            </HBox>

            <HBox fx:id="selectionToolbar" alignment="CENTER_LEFT" prefHeight="60"
                  styleClass="selection-header" visible="false" managed="false">
                <padding><Insets left="20" right="20"/></padding>
                <Button text="取消" onAction="#exitSelectionMode" styleClass="text-btn"/>
                <Region prefWidth="20"/>
                <Label fx:id="selectionCountLabel" text="已选择 0 条" styleClass="selection-count-text"/>
                <Region HBox.hgrow="ALWAYS"/>

                <HBox spacing="15">
                    <Button text="转发" onAction="#handleBatchForward" styleClass="action-btn"/>
                    <Button text="复制" onAction="#handleBatchCopy" styleClass="action-btn"/>
                    <Button text="删除" onAction="#handleBatchDelete" styleClass="action-btn-danger"/>
                </HBox>
            </HBox>

            <ListView fx:id="chatListView" VBox.vgrow="ALWAYS" styleClass="chat-list-view"/>
        </VBox>
    </center>

    <bottom>
        <VBox styleClass="input-area-pane">
            <ScrollPane fx:id="pendingFileScroll" vbarPolicy="NEVER" hbarPolicy="AS_NEEDED"
                        styleClass="pending-scroll" fitToHeight="true"
                        maxHeight="0" visible="false">
                <HBox fx:id="pendingFileBox" spacing="10" style="-fx-padding: 10;"/>
            </ScrollPane>

            <HBox spacing="15" alignment="CENTER_LEFT" prefHeight="70">
                <padding>
                    <Insets left="20" right="20"/>
                </padding>

                <Button text="📷" onAction="#handleSelectFileAction" styleClass="icon-btn"/>

                <TextField fx:id="messageInputField" HBox.hgrow="ALWAYS"
                           promptText="发送加密消息..."
                           onAction="#handleSendMessageAction"
                           styleClass="msg-input"/>

                <Button fx:id="burnButton" text="⌛"
                        onAction="#handleBurnMessageAction"
                        styleClass="icon-btn, burn-btn">
                    <tooltip><Tooltip text="阅后即焚模式"/></tooltip>
                </Button>

                <Button fx:id="sendButton" text="发送"
                        onAction="#handleSendMessageAction"
                        styleClass="send-btn" prefWidth="80"/>
            </HBox>
        </VBox>
    </bottom>

</BorderPane>