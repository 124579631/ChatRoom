<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Text?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.my.chatroom.ChatController"
            prefHeight="600.0" prefWidth="800.0"
            styleClass="root">

    <left>
        <VBox style="-fx-background-color: #2a2a2a;" prefWidth="200" spacing="10">
            <padding>
                <Insets top="10" right="5" bottom="10" left="5"/>
            </padding>
            <Text text="在线用户" styleClass="header-title" fill="#00ff00" style="-fx-font-size: 20px;"/>

            <ListView fx:id="userListView" styleClass="user-list-view" VBox.vgrow="ALWAYS"/>

            <Button text="📢 进入公共群聊"
                    maxWidth="Infinity"
                    style="-fx-background-color: #ff9900; -fx-text-fill: black; -fx-font-weight: bold;"
                    onAction="#handleJoinPublicChat"/>
        </VBox>
    </left>

    <center>
        <VBox>
            <HBox alignment="CENTER_LEFT" spacing="10" style="-fx-background-color: #2a2a2a; -fx-padding: 10;">
                <Text fx:id="chatTargetLabel" text="[未选择聊天]" styleClass="header-title" fill="#ffffff" style="-fx-font-size: 20px;"/>

                <Button fx:id="connectButton" text="🔐 建立加密通道"
                        onAction="#handleConnectAction"
                        style="-fx-background-color: #00ff00; -fx-text-fill: black; -fx-font-weight: bold; -fx-font-size: 12px;"
                        visible="false"/>

            </HBox>

            <ListView fx:id="chatListView" VBox.vgrow="ALWAYS" styleClass="chat-list-view"/>
        </VBox>
    </center>

    <bottom>
        <HBox spacing="10" alignment="CENTER_LEFT" styleClass="bottom-bar">
            <padding>
                <Insets top="10" right="10" bottom="10" left="10"/>
            </padding>

            <TextField fx:id="messageInputField" HBox.hgrow="ALWAYS"
                       promptText="在此输入加密消息..."
                       onAction="#handleSendMessageAction"
                       styleClass="message-input-field"/>

            <Button fx:id="sendButton" text="发 送"
                    onAction="#handleSendMessageAction"
                    styleClass="button" prefWidth="80"/>

            <Button fx:id="burnButton" text="🔥 阅后即焚"
                    onAction="#handleBurnMessageAction"
                    style="-fx-background-color: #ff4d4d; -fx-text-fill: white; -fx-font-weight: bold;"
                    prefWidth="100"/>
        </HBox>
    </bottom>

</BorderPane>